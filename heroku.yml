setup:
  addons:
    - plan: heroku-postgresql
  config:
    RAILS_ENV: production
build:
  docker:
    web: Dockerfile
  config:
    WORKDIR: app
run:
  web: bash -c "rm -f tmp/pids/server.pid &&
               bundle exec rails s -p 3000 -b '0.0.0.0'"